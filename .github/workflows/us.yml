name: US Stock Scanner KST

on:
  schedule:
    - cron: '0 21 * * *' # 韩国时间 06:00
  workflow_dispatch:

jobs:
  build-and-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install yfinance pandas openpyxl requests googletrans==4.0.0-rc1

      - name: Prepare Directories
        run: |
          mkdir -p output
          mkdir -p ticker_storage
          mkdir -p cache

      - name: Run US Scanner
        run: python scanner.py --use-data-cache

      - name: Send US Excel To Telegram
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
        run: python notif.py us

      - name: Upload Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: us-stock-report
          path: output/*
